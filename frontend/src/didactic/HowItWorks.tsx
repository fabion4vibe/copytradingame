// TODO: implementato in TASK_12
// Pannello laterale con spiegazione completa del modello economico della piattaforma
interface HowItWorksProps {
  open: boolean;
  onClose: () => void;
}
export function HowItWorks({ open: _open, onClose: _onClose }: HowItWorksProps) {
  return null;
}
